# ===========================================
# TradingAgents API Configuration
# ===========================================

# API Authentication
# Comma-separated list of valid API keys for accessing the API
# Leave empty for development mode (no auth required)
TRADING_API_KEYS=

# ===========================================
# LLM Configuration
# ===========================================

# LLM Provider: openai, anthropic, ollama, openrouter, openai-compatible
LLM_PROVIDER=openai

# Models
LLM_DEEP_THINK_MODEL=gpt-4o
LLM_QUICK_THINK_MODEL=gpt-4o-mini

# Custom backend URL (for openai-compatible providers)
# LLM_BACKEND_URL=https://open.bigmodel.cn/api/paas/v4

# LLM API Key
LLM_API_KEY=

# ===========================================
# Embedding Configuration
# ===========================================

# Embedding Provider: same_as_llm, openai, gemini, local, disabled
EMBEDDING_PROVIDER=same_as_llm

# Embedding Model (leave empty for auto-detect)
# EMBEDDING_MODEL=embedding-3

# Custom Embedding Backend URL (leave empty to use LLM_BACKEND_URL)
# EMBEDDING_BACKEND_URL=

# ===========================================
# Data Source API Keys
# ===========================================

# Alpha Vantage (for market data and news)
# Get your key from: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=

# Google Gemini API (for Google Search grounding and embeddings)
# Get your key from: https://aistudio.google.com/
# Used for: news search (google vendor), embeddings (gemini provider)
GOOGLE_API_KEY=

# ===========================================
# Reports Output
# ===========================================

# Directory for saving reports (set by docker-compose)
# REPORTS_OUTPUT_DIR=/app/reports

# ===========================================
# Cloudflare R2 Storage (Optional)
# ===========================================
# If configured, reports will be uploaded to R2 in addition to local storage.
# This enables cloud-based storage for better accessibility and durability.
# Requires: pip install boto3

# Your Cloudflare account ID (found in R2 dashboard)
# R2_ACCOUNT_ID=your_account_id

# R2 API credentials (create in R2 dashboard > Manage R2 API Tokens)
# R2_ACCESS_KEY_ID=your_access_key_id
# R2_SECRET_ACCESS_KEY=your_secret_access_key

# R2 bucket name for storing reports
# R2_BUCKET_NAME=trading-reports

# Optional: Custom endpoint URL (auto-generated from account_id if not set)
# R2_ENDPOINT_URL=https://your_account_id.r2.cloudflarestorage.com

# Optional: Presigned URL expiry in seconds (default: 3600 = 1 hour)
# R2_PRESIGNED_URL_EXPIRY=3600

# Optional: Public URL base for permanent public access (no expiry)
# Enable public access in R2 dashboard first, then use:
# - R2.dev URL: https://pub-{hash}.r2.dev
# - Custom domain: https://files.example.com
# R2_PUBLIC_URL=

# ===========================================
# Discord Notifications (Optional)
# ===========================================
# Send notifications to Discord when analysis completes.
# Create a webhook in Discord: Server Settings > Integrations > Webhooks

# Discord webhook URL
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# ===========================================
# Portfolio Manager (Optional)
# ===========================================
# Enable personalized portfolio-aware trading recommendations.
# Requires Google Sheets API credentials for portfolio storage.

# Enable portfolio manager (true/false)
# PORTFOLIO_MANAGER_ENABLED=false

# Path to Google Sheets service account credentials JSON file
# Create credentials at: https://console.cloud.google.com/
# GOOGLE_SHEETS_CREDENTIALS=/path/to/credentials.json

# Google Sheet ID for portfolio storage
# Find in sheet URL: https://docs.google.com/spreadsheets/d/{SHEET_ID}/edit
# GOOGLE_SHEET_ID=

# Sheet name (default: "Trading Portfolio")
# GOOGLE_SHEET_NAME=Trading Portfolio

# Maximum position size as percentage of portfolio (default: 0.20 = 20%)
# PORTFOLIO_MAX_POSITION_SIZE=0.20

# Minimum cash reserve as percentage of portfolio (default: 0.10 = 10%)
# PORTFOLIO_MIN_CASH_RESERVE=0.10

# ===========================================
# Logging Configuration
# ===========================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log format: json (for Docker/aggregation) or text (human readable)
LOG_FORMAT=json

# ===========================================
# Database & Worker Configuration
# ===========================================

# SQLite database path (set by docker-compose for containerized deployment)
# DB_PATH=./data/jobs.db

# Maximum concurrent workers
MAX_WORKERS=2

# Job timeout in seconds (default: 1 hour)
JOB_TIMEOUT=3600
