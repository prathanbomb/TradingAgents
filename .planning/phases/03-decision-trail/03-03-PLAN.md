---
phase: 03-decision-trail
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tradingagents/observability/trail/queries.py
  - tradingagents/observability/trail/__init__.py
  - tradingagents/observability/storage/sqlite_backend.py
autonomous: true
requirements:
  - TRAIL-03
must_haves:
  truths:
    - "User can filter and search through past decision trails"
  artifacts:
    - path: "tradingagents/observability/trail/queries.py"
      provides: "TrailQuery class for filtering and searching trails"
      min_lines: 120
      exports: ["TrailQuery"]
    - path: "tradingagents/observability/storage/sqlite_backend.py"
      provides: "Extended query methods for trail filtering"
      min_lines: 60
  key_links:
    - from: "tradingagents/observability/trail/queries.py"
      to: "tradingagents/observability/storage/sqlite_backend.py"
      via: "SQLiteDecisionStore for filtered trail queries"
      pattern: "store.get_decision_records|store.get_unique_run_ids"
    - from: "tradingagents/observability/trail/queries.py"
      to: "tradingagents/observability/trail/builder.py"
      via: "TrailBuilder to construct DecisionTrail from filtered results"
      pattern: "TrailBuilder\\(store\\).build_trail"
---

<objective>
Implement TrailQuery interface for filtering and searching decision trails by ticker, date range, agent type, and confidence thresholds.

Purpose: Enable users to find specific decision trails from historical data using flexible filters. TrailQuery wraps SQLite queries and TrailBuilder to provide a simple search interface.

Output: TrailQuery class with get_trails() and search_trails() methods for filtered trail discovery
</objective>

<execution_context>
@/Users/prathanbomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prathanbomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-decision-trail/03-RESEARCH.md
@.planning/phases/03-decision-trail/03-01-PLAN.md
@.planning/phases/03-decision-trail/03-02-PLAN.md
@.planning/REQUIREMENTS.md
@tradingagents/observability/storage/sqlite_backend.py

# Phase 1 & 2 Context

From SQLiteDecisionStore (Phase 1/2):
- get_decision_records(ticker, start_date, end_date, limit) with flexible filters
- Existing indexes: idx_ticker_date, idx_run_id_decisions, idx_agent_name_decisions
- Pattern: Dynamic WHERE clause construction for optional filters

From Research (03-RESEARCH.md):
- Trail filtering by ticker, date range, agent_type
- Time-series queries use proper indexes for efficiency
- Search combines SQL filters with in-memory TrailBuilder construction
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_unique_run_ids method to SQLiteDecisionStore</name>
  <files>tradingagents/observability/storage/sqlite_backend.py</files>
  <action>
Add method to discover unique run_ids matching filters:

get_unique_run_ids(
    self,
    ticker: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    agent_name: Optional[str] = None,
    limit: int = 100
) -> List[str]

Implementation:
- Query: SELECT DISTINCT run_id FROM decision_records WHERE 1=1
- Add filters: ticker = ?, trade_date >= ?, trade_date <= ?, agent_name = ?
- Order by trade_date DESC, run_id DESC
- Return list of run_id strings (not full records)

Pattern: Follow existing get_decision_records() filter construction pattern.

CRITICAL: Use idx_ticker_date and idx_run_id_decisions indexes for efficiency.
  </action>
  <verify>
python -c "from tradingagents.observability.storage.sqlite_backend import SQLiteDecisionStore; store = SQLiteDecisionStore(':memory:'); result = store.get_unique_run_ids(limit=10); print('Method works, returned:', type(result), 'empty:', len(result) == 0)"
  </verify>
  <done>SQLiteDecisionStore extended with run_id discovery method using indexed filters</done>
</task>

<task type="auto">
  <name>Task 2: Create TrailQuery class with filter methods</name>
  <files>tradingagents/observability/trail/queries.py</files>
  <action>
Create TrailQuery class for trail discovery and filtering:

Constructor:
- __init__(self, store: SQLiteDecisionStore, builder: Optional[TrailBuilder] = None)
- Store reference, create builder if not provided

Core methods:

1. get_trails(
    self,
    ticker: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    agent_name: Optional[str] = None,
    limit: int = 50
  ) -> List[str]
  - Returns list of run_ids matching filters
  - Calls store.get_unique_run_ids() with filters
  - Use case: List available trails before building them

2. get_trail(
    self,
    run_id: str
  ) -> Optional[DecisionTrail]
  - Builds complete DecisionTrail for run_id
  - Delegates to builder.build_trail(run_id)
  - Returns None if run_id not found

3. search_trails(
    self,
    query: str,
    ticker: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    limit: int = 20
  ) -> List[Dict[str, Any]]
  - Full-text search in reasoning fields
  - Query: SELECT run_id, ticker, trade_date, agent_name, reasoning FROM decision_records WHERE reasoning LIKE ?
  - Use pattern matching: %query%
  - Returns summary dicts (not full trails)

DO NOT add:
- Complex search ranking (simple LIKE matching for v1)
- Semantic search (defer to future)
- Boolean search operators (AND/OR/NOT)
  </action>
  <verify>
python -c "from tradingagents.observability.trail.queries import TrailQuery; from tradingagents.observability.storage.sqlite_backend import SQLiteDecisionStore; store = SQLiteDecisionStore(':memory:'); query = TrailQuery(store); print('TrailQuery initialized:', type(query))"
  </verify>
  <done>TrailQuery class created with get_trails(), get_trail(), and search_trails() methods</done>
</task>

<task type="auto">
  <name>Task 3: Add confidence-based filtering to TrailQuery</name>
  <files>tradingagents/observability/trail/queries.py</files>
  <action>
Add confidence-filtered query method to TrailQuery:

get_trails_by_confidence(
    self,
    confidence_min: Optional[float] = None,
    confidence_max: Optional[float] = None,
    ticker: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    limit: int = 50
) -> List[str]

Implementation:
- Leverage existing store.get_decision_records_by_confidence() from Phase 2
- Extract unique run_ids from filtered records
- Return deduplicated run_id list

Helper method:
- _deduplicate_run_ids(records: List[Dict]) -> List[str]
  - Extract run_id from each record
  - Use set() for deduplication
  - Return sorted list

Pattern: Build on Phase 2 confidence infrastructure (idx_confidence index already exists).
  </action>
  <verify>
python -c "from tradingagents.observability.trail.queries import TrailQuery; import inspect; print('Method exists:', 'get_trails_by_confidence' in dir(TrailQuery))"
  </verify>
  <done>Confidence-based filtering added to TrailQuery for finding high/low confidence decisions</done>
</task>

<task type="auto">
  <name>Task 4: Export TrailQuery from trail module</name>
  <files>tradingagents/observability/trail/__init__.py</files>
  <action>
Update trail module __init__.py to export TrailQuery:

Add: from .queries import TrailQuery

Update __all__ list to include: DecisionTrail, TrailNode, TrailEdge, TrailBuilder, TrailQuery

Pattern: Complete the public API for trail subsystem (models + builder + queries).
  </action>
  <verify>
python -c "from tradingagents.observability.trail import TrailQuery; print('TrailQuery exported successfully')"
  </verify>
  <done>TrailQuery exported from trail module completing the public API</done>
</task>

</tasks>

<verification>
1. TrailQuery.get_trails() returns run_id lists matching filters
2. TrailQuery.get_trail(run_id) builds complete DecisionTrail
3. TrailQuery.search_trails() finds reasoning matching query string
4. TrailQuery.get_trails_by_confidence() filters by confidence range
5. All methods use existing SQLite indexes for efficiency
6. Module exports include TrailQuery alongside other trail components
</verification>

<success_criteria>
- TrailQuery provides flexible search/filter interface for decision trails
- Users can discover trails by ticker, date, agent, confidence
- Full-text search works on reasoning fields
- get_trail() bridges query results to TrailBuilder for complete trail construction
- Public API complete (models, builder, queries all exported)
</success_criteria>

<output>
After completion, create `.planning/phases/03-decision-trail/03-03-SUMMARY.md`
</output>
