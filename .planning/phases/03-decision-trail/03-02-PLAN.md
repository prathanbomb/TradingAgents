---
phase: 03-decision-trail
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tradingagents/observability/trail/builder.py
  - tradingagents/observability/trail/__init__.py
  - tradingagents/observability/storage/sqlite_backend.py
autonomous: true
requirements:
  - TRAIL-01
  - TRAIL-02
  - TRAIL-04
must_haves:
  truths:
    - "User can view a timeline of decision flow from data input to final recommendation"
    - "Timeline shows each agent's action and output in chronological order"
    - "System displays the causal chain from data → analysis → debate → decision"
  artifacts:
    - path: "tradingagents/observability/trail/builder.py"
      provides: "TrailBuilder class for constructing decision trails"
      min_lines: 150
      exports: ["TrailBuilder"]
    - path: "tradingagents/observability/storage/sqlite_backend.py"
      provides: "Extended query methods for trail data retrieval"
      min_lines: 50
      exports: ["get_decision_records_by_run_id", "get_agent_events_by_run_id"]
  key_links:
    - from: "tradingagents/observability/trail/builder.py"
      to: "tradingagents/observability/storage/sqlite_backend.py"
      via: "SQLiteDecisionStore for retrieving events and records by run_id"
      pattern: "store.get_decision_records_by_run_id|store.get_agent_events_by_run_id"
    - from: "tradingagents/observability/trail/builder.py"
      to: "tradingagents/observability/trail/models.py"
      via: "Construct DecisionTrail from DecisionRecord and AgentEvent"
      pattern: "DecisionTrail\\(nodes=nodes, edges=edges\\)"
    - from: "tradingagents/observability/storage/sqlite_backend.py"
      to: "tradingagents/observability/models/decision_record.py"
      via: "Query and deserialize DecisionRecord from SQLite"
      pattern: "DecisionRecord.from_dict"
---

<objective>
Build TrailBuilder to aggregate DecisionRecord and AgentEvent by run_id into DecisionTrail objects with chronological ordering and causal chain reconstruction.

Purpose: Transform stored observability data into structured decision trails that users can query and visualize. TrailBuilder orchestrates data retrieval from SQLite and constructs the timeline/causal graph.

Output: TrailBuilder class with build_trail() method that returns complete DecisionTrail for any run_id
</objective>

<execution_context>
@/Users/prathanbomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prathanbomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-decision-trail/03-RESEARCH.md
@.planning/phases/03-decision-trail/03-01-PLAN.md
@.planning/REQUIREMENTS.md
@tradingagents/observability/storage/sqlite_backend.py
@tradingagents/observability/models/decision_record.py
@tradingagents/observability/models/agent_event.py

# Phase 1 & 2 Context

From SQLiteDecisionStore (Phase 1):
- get_agent_events(run_id) fetches events by run_id
- get_decision_records(ticker, start_date, end_date) queries records
- Existing indexes: idx_run_id_decisions, idx_run_id_events for efficient queries

From Research (03-RESEARCH.md):
- TrailBuilder aggregates by run_id to reconstruct single execution timeline
- Chronological order via timestamp sorting
- Causal chains from state_transition events (from_state → to_state)
- Avoid N+1 query pattern: fetch all events/records for run_id in single query

From Plan 03-01:
- DecisionTrail, TrailNode, TrailEdge models define the target structure
- TrailNode for decision points, TrailEdge for causal links
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run_id query methods to SQLiteDecisionStore</name>
  <files>tradingagents/observability/storage/sqlite_backend.py</files>
  <action>
Add two query methods to SQLiteDecisionStore for efficient run_id-based trail retrieval:

1. get_decision_records_by_run_id(run_id: str) -> List[Dict[str, Any]]
   - Query: SELECT * FROM decision_records WHERE run_id = ? ORDER BY timestamp ASC
   - Uses idx_run_id_decisions index
   - Returns list of dicts (not DecisionRecord objects - keep it simple for store layer)

2. get_agent_events_by_run_id(run_id: str) -> List[Dict[str, Any]]
   - Query: SELECT * FROM agent_events WHERE run_id = ? ORDER BY timestamp ASC
   - Uses idx_run_id_events index
   - Returns list of dicts

Pattern: Follow existing get_agent_events() pattern but filter by run_id only.

CRITICAL: Both methods must ORDER BY timestamp ASC to support chronological aggregation without in-memory sorting.

DO NOT add:
- Pagination (not needed for v1 - single run fits in memory)
- Caching (SQLite indexes are fast enough)
- Filtering beyond run_id (TrailBuilder can filter if needed)
  </action>
  <verify>
python -c "from tradingagents.observability.storage.sqlite_backend import SQLiteDecisionStore; store = SQLiteDecisionStore(':memory:'); print('Methods exist:', hasattr(store, 'get_decision_records_by_run_id'), hasattr(store, 'get_agent_events_by_run_id'))"
  </verify>
  <done>SQLiteDecisionStore extended with run_id query methods that return chronologically ordered events</done>
</task>

<task type="auto">
  <name>Task 2: Create TrailBuilder class with build_trail method</name>
  <files>tradingagents/observability/trail/builder.py</files>
  <action>
Create TrailBuilder class with build_trail() method:

Constructor:
- __init__(self, store: SQLiteDecisionStore)
- Store reference for data retrieval

Core method:
- build_trail(self, run_id: str) -> DecisionTrail
  Steps:
  1. Fetch all decision_records for run_id using store.get_decision_records_by_run_id()
  2. Fetch all agent_events for run_id using store.get_agent_events_by_run_id()
  3. Convert dicts to DecisionRecord and AgentEvent objects using .from_dict()
  4. Sort all items by timestamp (already sorted from SQL but verify)
  5. Build TrailNode list from DecisionRecord objects
  6. Build TrailEdge list from AgentEvent objects with event_type="state_transition"
  7. Extract ticker/trade_date from first DecisionRecord
  8. Create DecisionTrail with nodes, edges, run_id, metadata

Helper method:
- _build_nodes_from_records(records: List[DecisionRecord]) -> List[TrailNode]
  - Map each DecisionRecord to TrailNode
  - Truncate reasoning to 200 chars
  - Set node_type="decision"

- _build_edges_from_events(events: List[AgentEvent]) -> List[TrailEdge]
  - Filter for event_type="state_transition"
  - Create TrailEdge from from_state/to_state
  - Set edge_type="state_transition"

CRITICAL: Handle missing data gracefully (empty run_id returns empty trail, not error).
  </action>
  <verify>
python -c "from tradingagents.observability.trail.builder import TrailBuilder; from tradingagents.observability.storage.sqlite_backend import SQLiteDecisionStore; store = SQLiteDecisionStore(':memory:'); builder = TrailBuilder(store); print('TrailBuilder initialized:', type(builder))"
  </verify>
  <done>TrailBuilder class created with build_trail() that aggregates run_id events into DecisionTrail</done>
</task>

<task type="auto">
  <name>Task 3: Implement causal chain reconstruction logic</name>
  <files>tradingagents/observability/trail/builder.py</files>
  <action>
Add causal chain reconstruction method to TrailBuilder:

- _reconstruct_causal_chain(self, nodes: List[TrailNode], edges: List[TrailEdge]) -> None
  - This method enhances edges with "influences" relationships
  - For each edge where target is an agent_name in nodes:
    - Find source node with agent_name == edge.source
    - If found, create additional TrailEdge with edge_type="output_influences"
    - This links agent outputs to the next agent that uses them

Pattern: State transitions show LangGraph flow (bull_researcher → investment_judge). Output influences show data flow (Bull Researcher's output influences Investment Judge's decision).

DO NOT implement:
- Graph algorithms (topological sort, cycle detection)
- Machine learning for causal inference
- Complex dependency analysis beyond state_transition edges
  </action>
  <verify>
python -c "from tradingagents.observability.trail.builder import TrailBuilder; import inspect; print('Method exists:', 'reconstruct_causal_chain' in dir(TrailBuilder))"
  </verify>
  <done>Causal chain reconstruction adds output_influences edges to show data flow between agents</done>
</task>

<task type="auto">
  <name>Task 4: Export TrailBuilder from trail module</name>
  <files>tradingagents/observability/trail/__init__.py</files>
  <action>
Update trail module __init__.py to export TrailBuilder:

Add: from .builder import TrailBuilder

Expose in __all__ list alongside DecisionTrail, TrailNode, TrailEdge.

Pattern: Follow existing pattern from confidence/__init__.py (exports ConfidenceScorer, ConfidenceAggregator).
  </action>
  <verify>
python -c "from tradingagents.observability.trail import TrailBuilder; print('TrailBuilder exported successfully')"
  </verify>
  <done>TrailBuilder exported from trail module for public API access</done>
</task>

</tasks>

<verification>
1. TrailBuilder.build_trail(run_id) returns DecisionTrail with populated nodes and edges
2. SQLite queries use run_id indexes for efficiency (<100ms for typical run)
3. Chronological ordering preserved (timestamp ASC in SQL queries)
4. Causal edges created from state_transition events
5. Empty runs handled gracefully (returns trail with 0 nodes/edges)
6. All exports accessible from tradingagents.observability.trail
</verification>

<success_criteria>
- TrailBuilder can construct DecisionTrail from run_id
- Nodes contain all agent decisions with truncated reasoning
- Edges capture state transitions for causal chain
- Chronological order maintained throughout trail
- Module exports include TrailBuilder alongside models
</success_criteria>

<output>
After completion, create `.planning/phases/03-decision-trail/03-02-SUMMARY.md`
</output>
