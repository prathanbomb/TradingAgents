---
phase: 03-decision-trail
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - tradingagents/observability/trail/display.py
  - tradingagents/observability/trail/__init__.py
autonomous: false
requirements:
  - TRAIL-01
  - TRAIL-02
  - TRAIL-04
must_haves:
  truths:
    - "User can view a timeline of decision flow from data input to final recommendation"
    - "Timeline shows each agent's action and output in chronological order"
    - "System displays the causal chain from data → analysis → debate → decision"
  artifacts:
    - path: "tradingagents/observability/trail/display.py"
      provides: "TrailRenderer for text-based decision trail visualization"
      min_lines: 150
      exports: ["TrailRenderer"]
  key_links:
    - from: "tradingagents/observability/trail/display.py"
      to: "tradingagents/observability/trail/models.py"
      via: "Import DecisionTrail for rendering"
      pattern: "from tradingagents.observability.trail.models import DecisionTrail"
    - from: "tradingagents/observability/trail/display.py"
      to: "tradingagents/observability/trail/queries.py"
      via: "Combine with TrailQuery for end-to-end trail retrieval and display"
      pattern: "TrailQuery\\(\\).get_trail"
---

<objective>
Build text-based decision trail visualization with chronological timeline view and causal chain display, enabling users to understand decision flow without graphical UI.

Purpose: Provide immediate value from Phase 3 by making decision trails viewable in terminal/CLI. Text-based visualization is pragmatic first step before Phase 6 (UI Layer).

Output: TrailRenderer class with render_timeline() and render_causal_chain() methods for formatted trail display
</objective>

<execution_context>
@/Users/prathanbomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prathanbomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-decision-trail/03-RESEARCH.md
@.planning/phases/03-decision-trail/03-01-PLAN.md
@.planning/phases/03-decision-trail/03-02-PLAN.md
@.planning/phases/03-decision-trail/03-03-PLAN.md
@.planning/REQUIREMENTS.md

# Phase 1 & 2 Context

From Research (03-RESEARCH.md):
- Progressive disclosure: summary view first, details on demand
- Text-based causal chain sufficient for v1 (graph visualization deferred)
- Show truncated reasoning in timeline, full reasoning available on expansion
- Avoid information overload (wall of events anti-pattern)

From Plans 03-01/03-02/03-03:
- DecisionTrail contains nodes (agent decisions) and edges (causal links)
- TrailBuilder constructs trails from run_id
- TrailQuery finds and filters trails
- Models have helper methods for filtering (get_agent_nodes, get_outgoing_edges)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrailRenderer class with timeline formatting</name>
  <files>tradingagents/observability/trail/display.py</files>
  <action>
Create TrailRenderer class for text-based trail visualization:

Constructor:
- __init__(self, show_full_reasoning: bool = False, max_reasoning_length: int = 200)
- Configure display preferences

Core method:
- render_timeline(self, trail: DecisionTrail) -> str
  - Returns formatted string showing chronological decision flow
  - Format per node:
    ```
    [timestamp] {agent_name} ({agent_type})
    Decision: {decision}
    Confidence: {confidence:.2f}
    Reasoning: {truncated_reasoning}
    ---
    ```
  - Sort nodes by timestamp (already sorted from builder)
  - Section header: "Decision Trail for {ticker} on {trade_date} (Duration: {duration}s)"
  - Footer: "Total decisions: {len(nodes)}"

Helper methods:
- _format_node(self, node: TrailNode) -> str
  - Format individual node with indentation and separators
  - Truncate reasoning to max_reasoning_length if show_full_reasoning=False
  - Format confidence as percentage (0.85 → "85%")
  - Handle missing confidence (display "N/A")

- _format_duration(self, started_at: str, completed_at: str) -> str
  - Calculate duration between timestamps
  - Return human-readable string ("12.5s", "2m 15s")
  </action>
  <verify>
python -c "from tradingagents.observability.trail.display import TrailRenderer; renderer = TrailRenderer(); print('TrailRenderer initialized:', type(renderer)); print('Has render_timeline:', hasattr(renderer, 'render_timeline'))"
  </verify>
  <done>TrailRenderer created with render_timeline() for chronological decision display</done>
</task>

<task type="auto">
  <name>Task 2: Implement causal chain visualization</name>
  <files>tradingagents/observability/trail/display.py</files>
  <action>
Add causal chain rendering method to TrailRenderer:

- render_causal_chain(self, trail: DecisionTrail) -> str
  - Returns formatted string showing causal flow between agents
  - Format per edge:
    ```
    {source} → {target}
      via: {agent_name}
      type: {edge_type}
    ```
  - Group edges by target to show multiple inputs
  - Section header: "Causal Chain: How Decisions Emerged"
  - Indentation for visual hierarchy

Helper method:
- _group_edges_by_target(self, edges: List[TrailEdge]) -> Dict[str, List[TrailEdge]]
  - Group incoming edges for each target node
  - Return dict mapping target -> list of incoming edges
  - Use for showing "X influenced Y" relationships

Display pattern:
- Show state transitions as direct arrows (bull_researcher → investment_judge)
- Show output influences as nested (via {agent_name})
- Highlight final decision (portfolio_manager/risk_judge) with "***"
  </action>
  <verify>
python -c "from tradingagents.observability.trail.display import TrailRenderer; import inspect; print('Method exists:', 'render_causal_chain' in dir(TrailRenderer))"
  </verify>
  <done>Causal chain visualization added to TrailRenderer showing decision flow</done>
</task>

<task type="auto">
  <name>Task 3: Add full trail rendering combining timeline and causal views</name>
  <files>tradingagents/observability/trail/display.py</files>
  <action>
Add comprehensive rendering method to TrailRenderer:

- render_trail(self, trail: DecisionTrail) -> str
  - Combines timeline and causal chain in single output
  - Format:
    1. Header with ticker, date, run_id
    2. Timeline section (render_timeline output)
    3. Separator line
    4. Causal chain section (render_causal_chain output)
    5. Summary footer (total nodes, total edges, unique agents)

Helper method:
- _get_summary_stats(self, trail: DecisionTrail) -> Dict[str, Any]
  - Compute: total_nodes, total_edges, unique_agents, duration
  - Return dict for footer rendering

Add method:
- render_trail_markdown(self, trail: DecisionTrail) -> str
  - Same content as render_trail() but formatted as Markdown
  - Use headers (###), bullets (-), code blocks (```) for structure
  - Enables export to .md files for documentation
  </action>
  <verify>
python -c "from tradingagents.observability.trail.display import TrailRenderer; import inspect; print('Methods exist:', 'render_trail' in dir(TrailRenderer), 'render_trail_markdown' in dir(TrailRenderer))"
  </verify>
  <done>Full trail rendering combines timeline and causal views with Markdown export</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete text-based decision trail visualization with timeline and causal chain views</what-built>
  <how-to-verify>
1. Create a test trading analysis run to generate decision data:
   ```bash
   cd /Users/prathanbomb/Documents/workspace-python/trading-agents
   python -m tradingagents.scripts.run_scheduled_analysis --ticker AAPL --date 2026-02-27
   ```

2. Run trail viewer script to display decision trail:
   ```bash
   python -c "
   from tradingagents.observability.storage.sqlite_backend import SQLiteDecisionStore
   from tradingagents.observability.trail import TrailBuilder, TrailRenderer

   store = SQLiteDecisionStore('./data/observability.db')
   builder = TrailBuilder(store)

   # Get most recent run_id
   run_ids = store.get_unique_run_ids(limit=1)
   if run_ids:
       trail = builder.build_trail(run_ids[0])
       renderer = TrailRenderer(show_full_reasoning=False)
       print(renderer.render_trail(trail))
   else:
       print('No trails found. Run an analysis first.')
   "
   ```

3. Verify output shows:
   - Header with ticker, date, duration
   - Chronological timeline of agent decisions with confidence scores
   - Causal chain showing which agent influenced which
   - Summary footer with total decisions and unique agents

Expected behavior:
- Timeline is readable and not overwhelming (progressive disclosure)
- Causal arrows clearly show decision flow (e.g., "Bull Researcher → Investment Judge")
- Confidence scores displayed as percentages
- Truncated reasoning prevents wall-of-text (200 chars max)
  </how-to-verify>
  <resume-signal>Type "approved" if timeline and causal chain display correctly, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Export TrailRenderer from trail module</name>
  <files>tradingagents/observability/trail/__init__.py</files>
  <action>
Update trail module __init__.py to export TrailRenderer:

Add: from .display import TrailRenderer

Update __all__ list to include all components: DecisionTrail, TrailNode, TrailEdge, TrailBuilder, TrailQuery, TrailRenderer

This completes the full trail subsystem public API: models (03-01), builder (03-02), queries (03-03), display (03-04).
  </action>
  <verify>
python -c "from tradingagents.observability.trail import TrailRenderer; print('TrailRenderer exported successfully'); from tradingagents.observability.trail import *; print('All exports work:', DecisionTrail, TrailBuilder, TrailQuery, TrailRenderer)"
  </verify>
  <done>TrailRenderer exported from trail module completing the decision trail subsystem</done>
</task>

</tasks>

<verification>
1. TrailRenderer.render_trail() produces readable timeline with chronological decisions
2. Causal chain displays agent-to-agent influence clearly
3. Progressive disclosure prevents information overload (truncated reasoning)
4. Markdown export works for documentation
5. Checkpoint confirms visualization is comprehensible to human users
6. Full trail API exported (models, builder, queries, display)
</verification>

<success_criteria>
- Users can view decision trails in terminal/CLI without graphical UI
- Timeline shows chronological order of agent decisions
- Causal chain reveals which outputs influenced subsequent decisions
- Text formatting is readable and not overwhelming
- All TRAIL-01, TRAIL-02, TRAIL-04 requirements satisfied
- Trail subsystem complete and ready for Phase 4 (Debate Explorer)
</success_criteria>

<output>
After completion, create `.planning/phases/03-decision-trail/03-04-SUMMARY.md`
</output>
