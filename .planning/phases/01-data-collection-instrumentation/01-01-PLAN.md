---
phase: 01-data-collection-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tradingagents/observability/models/decision_record.py
  - tradingagents/observability/models/agent_event.py
  - tradingagents/observability/__init__.py
  - tradingagents/observability/models/__init__.py
autonomous: true
requirements:
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "DecisionRecord data model exists with outcome tracking hooks"
    - "AgentEvent model captures agent execution events"
    - "Models are Pydantic-based for type safety"
    - "Models include confidence placeholder for Phase 2"
  artifacts:
    - path: "tradingagents/observability/models/decision_record.py"
      provides: "DecisionRecord Pydantic model with outcome hooks"
      min_lines: 50
    - path: "tradingagents/observability/models/agent_event.py"
      provides: "AgentEvent Pydantic model for event capture"
      min_lines: 30
  key_links:
    - from: "tradingagents/observability/models/decision_record.py"
      to: "tradingagents/backtracking/agent_tracker.py"
      via: "extends PredictionRecord pattern"
      pattern: "class DecisionRecord.*BaseModel"
---

<objective>
Create Pydantic data models for decision records and agent events with outcome tracking hooks built in from day one.

Purpose: Establish type-safe data structures that capture agent decisions with fields for later outcome correlation (Phase 5), following existing patterns from AgentTracker for consistency.
Output: DecisionRecord and AgentEvent Pydantic models with outcome_pending fields, confidence placeholders, and proper validation
</objective>

<execution_context>
@/Users/prathanbomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prathanbomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-collection-instrumentation/01-RESEARCH.md
@.planning/phases/01-data-collection-instrumentation/01-CONTEXT.md
@.planning/REQUIREMENTS.md

<tradingagents/backtracking/agent_tracker.py>
```python
# Existing PredictionRecord pattern to extend
@dataclass
class PredictionRecord:
    ticker: str
    trade_date: str
    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())

    # Outcome data (filled later)
    entry_price: Optional[float] = None
    exit_price: Optional[float] = None
    hold_days: int = 7
    return_pct: Optional[float] = None
    outcome_calculated: bool = False
```

<tradingagents/agents/utils/agent_states.py>
```python
# AgentState structure for context
class AgentState(MessagesState):
    company_of_interest: Annotated[str, "Company that we are interested in trading"]
    trade_date: Annotated[str, "What date we are trading at"]
    market_report: Annotated[str, "Report from the Market Analyst"]
    sentiment_report: Annotated[str, "Report from the Social Media Analyst"]
    investment_debate_state: Annotated[InvestDebateState, replace_value]
    risk_debate_state: Annotated[RiskDebateState, replace_value]
    # ... more fields
```
</context>

<tasks>

<task type="auto">
  <name>Create DecisionRecord model with outcome tracking hooks</name>
  <files>tradingagents/observability/models/decision_record.py</files>
  <action>
Create DecisionRecord Pydantic model extending the existing PredictionRecord pattern from agent_tracker.py with the following fields:

**Core context fields:**
- decision_id: str = Field(default_factory=lambda: str(uuid.uuid4()))
- ticker: str
- trade_date: str (YYYY-MM-DD format)
- timestamp: str = Field(default_factory=lambda: datetime.utcnow().isoformat())
- run_id: Optional[str] = None  # For grouping events from a single graph execution

**Agent decision fields:**
- agent_name: str
- agent_type: Literal["analyst", "researcher", "debater", "manager", "trader", "risk_judge", "portfolio_manager"]
- decision: str
- reasoning: str

**Outcome tracking hooks (DATA-03 requirement):**
- outcome_pending: bool = Field(default=True)
- entry_price: Optional[float] = None
- exit_price: Optional[float] = None
- hold_days: int = Field(default=7)
- return_pct: Optional[float] = None
- outcome_calculated: bool = Field(default=False)
- outcome_calculated_at: Optional[datetime] = None

**Confidence placeholder (for Phase 2):**
- confidence: Optional[float] = Field(default=None, ge=0.0, le=1.0)

**Debate/analysis capture fields:**
- bull_signal: Optional[TradingSignal] = None
- bear_signal: Optional[TradingSignal] = None
- risk_signal: Optional[TradingSignal] = None
- final_signal: TradingSignal = TradingSignal.UNKNOWN

**Metadata:**
- metadata: Dict[str, Any] = Field(default_factory=dict)

Use Pydantic BaseModel (not dataclass) for consistency with config models. Include json_encoders for datetime serialization. Add validators for date format and signal enums.
  </action>
  <verify>
python -c "from tradingagents.observability.models import DecisionRecord; r = DecisionRecord(ticker='AAPL', trade_date='2026-02-27', agent_name='test', agent_type='analyst', decision='BUY', reasoning='test'); print(f'DecisionRecord created: {r.decision_id}'); assert r.outcome_pending == True; assert r.hold_days == 7"
  </verify>
  <done>DecisionRecord model exists with all required fields, validates correctly, and extends existing patterns from AgentTracker</done>
</task>

<task type="auto">
  <name>Create AgentEvent model for execution event capture</name>
  <files>tradingagents/observability/models/agent_event.py</files>
  <action>
Create AgentEvent Pydantic model for capturing granular execution events during graph flow:

**Core fields:**
- event_id: str = Field(default_factory=lambda: str(uuid.uuid4()))
- event_type: Literal["llm_call", "tool_use", "state_transition", "error"]
- agent_name: str
- timestamp: str = Field(default_factory=lambda: datetime.utcnow().isoformat())
- run_id: Optional[str] = None  # Link to DecisionRecord

**Event-specific data:**
- data: Dict[str, Any] = Field(default_factory=dict)
- metadata: Dict[str, Any] = Field(default_factory=dict)

**For llm_call events:**
- model: Optional[str] = None
- prompt_tokens: Optional[int] = None
- completion_tokens: Optional[int] = None
- total_tokens: Optional[int] = None

**For tool_use events:**
- tool_name: Optional[str] = None
- tool_input: Optional[Dict[str, Any]] = None
- tool_output: Optional[str] = None

**For state_transition events:**
- from_state: Optional[str] = None
- to_state: Optional[str] = None

**For error events:**
- error_type: Optional[str] = None
- error_message: Optional[str] = None

Use Pydantic BaseModel with proper validators. Include a method to_dict() for serialization.
  </action>
  <verify>
python -c "from tradingagents.observability.models import AgentEvent; e = AgentEvent(event_type='llm_call', agent_name='bull_researcher', model='gpt-4o'); print(f'AgentEvent created: {e.event_id}'); assert e.event_type == 'llm_call'"
  </verify>
  <done>AgentEvent model exists with support for all event types, proper validation, and serialization methods</done>
</task>

<task type="auto">
  <name>Create observability module structure and exports</name>
  <files>tradingagents/observability/__init__.py, tradingagents/observability/models/__init__.py</files>
  <action>
Create the observability module structure with proper __init__.py files:

1. Create tradingagents/observability/__init__.py:
   - Import and expose DecisionRecord, AgentEvent
   - Add module-level docstring explaining the observability system
   - Version marker for future compatibility

2. Create tradingagents/observability/models/__init__.py:
   - Export DecisionRecord and AgentEvent
   - Add TradingSignal enum import from backtracking for convenience
   - Create type aliases for common model collections (e.g., DecisionRecords = List[DecisionRecord])

3. Add logging configuration for the observability module
  </action>
  <verify>
python -c "from tradingagents.observability import DecisionRecord, AgentEvent; from tradingagents.observability.models import TradingSignal; print('All exports accessible'); assert DecisionRecord is not None; assert AgentEvent is not None"
  </verify>
  <done>Observability module structure exists with clean exports and proper logging configuration</done>
</task>

</tasks>

<verification>
- Models import correctly from tradingagents.observability
- DecisionRecord validates with required fields and defaults
- AgentEvent captures all event type variations
- Outcome tracking hooks present (outcome_pending, entry_price, exit_price, etc.)
- Confidence placeholder field exists for Phase 2
- Models follow existing Pydantic patterns from config module
</verification>

<success_criteria>
- DecisionRecord can be instantiated with ticker, trade_date, agent_name, agent_type, decision, reasoning
- All outcome tracking fields default appropriately (outcome_pending=True, hold_days=7)
- AgentEvent supports llm_call, tool_use, state_transition, and error event types
- Both models serialize to JSON without errors
- Models are accessible from tradingagents.observability namespace
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-collection-instrumentation/01-01-SUMMARY.md` documenting:
- Created DecisionRecord and AgentEvent models
- Outcome tracking hooks architecture
- How models extend existing AgentTracker patterns
- Model validation rules and defaults
</output>
